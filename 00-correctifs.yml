---
  - name: Ensure SELinux is set to enforcing mode
    hosts: all
    # become: yes

    tasks:
    - name: Ajout de la commande ll
      lineinfile:
        path: ~/.bashrc
        line: "alias = 'ls -phAl'"
        insertafter: EOL